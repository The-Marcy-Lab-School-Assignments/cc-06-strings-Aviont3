//write your code here
const reverseString = (str) => {
  let reverseStr=""
    for(let i = str.length -1 ; i >= 0;i--){
        reverseStr += str[i]
    }
    return reverseStr
};
reverseZigZagString= () => {
    
    let reverseStr=""
    for(let i = str.length -1 ; i >= 0;i--){
        reverseStr += str[i]
        if 
}
